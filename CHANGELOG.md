# 更新日志

## 0.5.32

- 增加 `ensure_superuser` 管理命令，支持线上部署时自动创建或更新后台超级管理员。
- Railway Docker 启动流程在数据库迁移后自动执行超级管理员初始化。

## 0.5.31

- 增加 Railway/Vercel 生产环境的 CSRF 信任源配置。
- 增加反向代理 HTTPS 识别配置，修复后台登录 Forbidden 和 API 根路由生成 http 链接的问题。

## 0.5.30

- Railway 后端部署改为 Dockerfile 构建，使用 Python 3.11 slim 并安装 ffmpeg。
- 修复 Railway Nixpacks 构建阶段缺少 pip 导致部署失败的问题。

## 0.5.29

- 增加 Vercel 前端部署配置 `vercel.json`。
- 增加 Railway 后端部署配置 `railway.json`、`Procfile`、`nixpacks.toml`。
- 后端增加 PostgreSQL、Gunicorn、WhiteNoise 和生产静态文件配置。
- 文档补充 Vercel/Railway 环境变量和支付宝线上回调配置。

## 0.5.28

- 播放页自动把后端返回的相对 HLS 地址补全为 API 域名，避免前端端口请求媒体文件。

## 0.5.27

- 增加 Cloudflare R2 HLS 切片上传能力。
- HLS 转码完成后在 R2 配置完整时自动上传，并回写公网 `hls_url`。
- `transcode_hls` 命令增加 `--upload-r2-only`，支持只上传已有 HLS 目录。
- 补充 R2 上传测试和部署文档。

## 0.5.26

- 视频模型增加 HLS 播放地址和本地切片目录字段。
- 增加 ffmpeg 转 HLS 管理命令 `transcode_hls`。
- 上传课程视频后自动尝试转码为 HLS，缺少 ffmpeg 时保留原 MP4 播放。
- 前端播放页接入 `hls.js`，优先播放 HLS，兼容回退原视频地址。
- 预留 Cloudflare R2 环境变量和部署文档。

## 0.5.25

- 评论提交后直接显示，不再进入待审核状态。
- 课程付费从模拟支付升级为支付宝沙箱电脑网站支付。
- 支付宝异步通知验签成功后才更新订单状态并生成讲师收益。
- 增加支付宝沙箱环境变量和安装/API 文档说明。

## 0.5.24

- 修复个人中心切换身份时写入无效 demo token 的问题。
- Axios 请求遇到 JWT 无效或过期时自动清除本地登录状态，并提示重新登录。

## 0.5.23

- 首页课程区改为读取后端已发布课程数据。
- 首页热门课程榜单和今日推荐同步使用后端课程。
- 首页课程封面显示教师上传封面，鼠标悬停显示课程标题。

## 0.5.22

- 课程详情页增加模拟支付弹窗，支付成功后后端生成订单和讲师收益记录。
- 课程详情页增加评论区，支持登录用户提交评论并进入后台审核。
- 评论列表只展示后台审核通过的可见评论。

## 0.5.21

- 课程详情页显示教师上传的真实课程封面。
- 视频播放页将课程封面作为播放器封面图显示。

## 0.5.20

- 课程分类页课程卡片显示教师上传的课程封面图。
- 课程标题改为鼠标悬停在封面上时以浮层显示。

## 0.5.19

- 上传作品弹窗增加课程封面前端预览。
- 课程上传结构升级为“章 / 节 / 视频”。
- 每章可添加多个节，每节可上传视频并设置是否试看。
- 播放页和课程详情目录改为按章展示节列表。

## 0.5.18

- 作品管理编辑保存接入后端课程更新接口。
- 作品管理删除接入后端真实删除接口。
- 课程目录增加播放按钮，试看章节可直接进入播放页。
- 收益中心接入后端讲师真实收益汇总和收益明细。

## 0.5.17

- 课程作品上传支持多章节结构。
- 每个章节可填写章节标题、章节简介、视频标题、视频文件和是否试看。
- 后端上传接口支持保存多个 Chapter 和 Video。
- 优化讲师上传课程作品弹窗的章节化表单排版。

## 0.5.16

- 完成课程播放页前后端联调。
- 播放页改为读取后端课程详情和章节视频文件。
- 后端上传的视频文件可在前端播放页通过 video 控件播放。

## 0.5.15

- 作品管理增加编辑弹窗和预览跳转交互。
- 讲师看板“我的作品”改为读取后端真实课程作品。
- 抽取课程作品状态和展示格式化工具，保证看板与作品管理展示一致。

## 0.5.14

- 增加认证讲师“我的课程作品”后端接口。
- 个人中心作品管理改为读取后端真实课程作品。
- 上传课程作品成功后自动切换并刷新作品管理列表。
- 公开课程列表和课程详情接入后端课程接口。
- 公开课程接口仅展示审核通过或已发布课程，待审核作品只在讲师个人中心显示。

## 0.5.13

- Django 后台课程作品增加管理员审核动作。
- 支持课程作品批量审核通过、审核通过并发布、驳回。
- 课程作品后台列表增加审核人、审核时间和发布时间显示。
- 增强课程作品筛选和搜索字段。

## 0.5.12

- 普通用户切换认证讲师身份时增加认证拦截弹窗。
- 未认证用户提示先申请认证教师，并自动进入讲师入驻申请页面。
- 认证讲师数据看板增加作品、收益、用户评价和待办事项模块。
- 优化讲师看板卡片排版和 hover 交互。

## 0.5.11

- 优化讲师认证申请提交成功弹窗。
- 明确提示用户耐心等待 2-3 个工作日，审核结果会通过邮箱通知。

## 0.5.10

- 优化讲师认证审核邮件为正式通知模板。
- 邮件内容增加昵称、账号、注册时间、认证方向和审核时间。
- 审核通过邮件增加恭喜文案和后续操作说明。

## 0.5.9

- 增加 Django SMTP 邮件环境变量配置。
- 讲师认证审核邮件通知支持通过 `backend/.env` 切换真实邮箱发送。
- 安装文档补充 163 邮箱和 QQ 邮箱 SMTP 配置示例。

## 0.5.8

- 统一前端全站视觉系统，调整为高端简洁的知识平台风格。
- 优化顶部导航、课程卡片、分类卡片、榜单、登录页、详情页、播放页和个人中心样式。
- 增加卡片、菜单、按钮、表格、分页等基础控件的 hover 反馈。
- 保持纯前端样式改造，不改变现有接口和业务逻辑。

## 0.5.7

- 讲师认证申请接入后端，支持试讲视频和资质文件上传。
- Django 后台讲师申请增加审核通过/驳回动作。
- 审核通过后自动开通认证讲师身份，并创建讲师档案。
- 审核结果通过邮件通知，当前使用 console email backend。
- 认证讲师作品上传接入后端，支持封面、视频文件和课程附件上传。
- 增加课程附件模型，支持多格式资料文件保存。
- 增加大文件上传相关配置。

## 0.5.6

- 登录账号识别增加昵称支持。
- 登录页文案改为账号、邮箱、手机号、昵称均可登录。
- 保留后端密码哈希存储方式，明确不保存明文密码。

## 0.5.5

- 注册增加手机号字段和前后端格式校验。
- 个人中心增加个人资料页面，展示昵称、账号、邮箱、手机号、注册时间和简介。
- 增加个人资料编辑弹窗，并通过后端 `/api/users/me/` 保存。
- 登录支持使用用户名、邮箱或手机号识别账号。

## 0.5.4

- 增加后端公开注册接口 `/api/users/register/`。
- 前端登录改为调用 JWT 接口。
- 前端注册改为调用 Django 后端并真实创建用户。
- Axios 默认后端地址设置为 `http://127.0.0.1:8000`。

## 0.5.3

- 删除 Vue 前端静态 `/admin` 管理后台，避免和 Django SimpleUI 后台混淆。
- 管理后台统一使用 Django 后端地址 `/admin/`。

## 0.5.2

- 安装并启用 Django SimpleUI 后台框架。
- 设置 Django Admin 中文站点标题。
- 为用户模型增加昵称字段。
- Django 根路径改为跳转到 Vue 前端地址。

## 0.5.1

- 修正后端字段设计。
- 为用户头像、课程封面、视频封面增加上传字段。
- 为章节视频增加本地上传、外部地址、云点播文件 ID、转码状态和文件大小字段。
- 为讲师申请增加试讲视频和资质文件上传字段。
- 为订单增加支付方式、第三方交易号、退款金额和退款时间字段。
- 为评论增加更完整的审核状态和审核备注。

## 0.5.0

- 新增 Django + DRF 后端项目。
- 配置 MySQL、CORS、JWT 登录、DRF 分页和自定义用户模型。
- 增加用户、讲师申请、讲师档案、课程分类、课程作品、章节视频、订单、收益、提现、评论、收藏等完整数据表。
- 增加 Django Admin、Serializer、ViewSet、API 路由和初始化 migration。

## 0.4.0

- 增加管理员后台全套静态页面。
- 包含后台首页、用户管理、讲师审核、作品审核、订单管理、提现审核、系统配置。
- 增加后台列表筛选、分页、操作弹窗、确认弹窗和静态数据卡片。

## 0.3.0

- 增加核心个人中心静态页面。
- 实现普通用户与认证讲师双身份自动切换。
- 普通用户展示讲师入驻申请；认证讲师展示数据看板、作品管理、收益中心、提现中心。
- 增加权限菜单自动隐藏和显示、表单校验、弹窗、数据卡片与静态表格。

## 0.2.0

- 完成知识分享平台静态页面：课程分类页、课程详情页、视频播放页、讲师公开主页、登录注册页。
- 增加筛选、分页、表单校验、弹窗和试看提示交互。
- 统一首页、课程、讲师和认证页面的教程平台视觉风格。

## 0.1.0

- 初始化 Vue3 + Vite + Element Plus 前端项目架构。
- 增加路由系统、登录权限守卫、Axios 请求封装和公共头部底部布局。
- 预留普通用户与讲师双身份权限结构。
